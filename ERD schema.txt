-- Exported from QuickDBD: https://www.quickdatabasediagrams.com/
-- NOTE! If you have used non-SQL datatypes in your design, you will have to change these here.
CREATE TABLE "Climate_Delhi" (
    "date" date   NOT NULL,
    "meantemp" INT   NOT NULL,
    "humidity" INT   NOT NULL,
    "wind_speed" INT   NOT NULL,
    "meanpressure" INT   NOT NULL,
    CONSTRAINT "pk_Climate_Delhi" PRIMARY KEY (
        "date"
     )
);
CREATE TABLE "Air_Quality_Delhi" (
    "date" date   NOT NULL,
    "no2" INT   NOT NULL,
    "o3" INT   NOT NULL,
    "pm10" INT   NOT NULL,
    "pm25" INT   NOT NULL,
    CONSTRAINT "pk_Air_Quality_Delhi" PRIMARY KEY (
        "date"
     )
);
ALTER TABLE "Air_Quality_Delhi" ADD CONSTRAINT "fk_Air_Quality_Delhi_date" FOREIGN KEY("date")
REFERENCES "Climate_Delhi" ("date");